version: '3.8'

services:
  php:
    build: .
    container_name: php_app
    volumes:
      - .:/app
    depends_on:
      - mysql

  mysql:
    image: mysql:8
    container_name: mysql_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: test_db
    ports:
      - "3308:3306"